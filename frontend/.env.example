# Local dev: keep as /api (proxied via package.json proxy field to localhost:5000)
# Railway: set this to your deployed backend URL in the Railway dashboard
REACT_APP_API_URL=https://your-backend.up.railway.app/api
